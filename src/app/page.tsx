export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="text-3xl font-semibold tracking-tight">gastospersonales</h1>
      <p className="max-w-md text-muted-foreground">
        Pre-MVP. The MVP scaffolding is being built — auth, capture flow, dashboard, and
        receipt OCR are coming next.
      </p>
    </main>
  );
}
